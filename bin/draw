#!/bin/sh -e
#
#   draw a term
#

eval "$(crud)"

# 100 = term padding
# 34  = window decoration size
# 6   = font width
# 13  = font height
[ "$W" -ge 106 ]
[ "$H" -ge 147 ]
w="$(( (W - 100) / 6 ))"
h="$(( (H - 100 - 34) / 13 ))"

st -g "${w}x$h+$X+$Y"
