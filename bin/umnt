#!/bin/sh -e

dis=$(lsblk -o 'name,mountpoint' | grep -e ' /' \
	| tr -d '─└├' | grep -iv ' /$\|boot' | menu)
[ "$dis" ] || exit 1
sudo umount "${dis#* }"
notify-send "Unmounted ${dis%% *}" "${dis#* }"
