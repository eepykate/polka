#!/usr/bin/env bash
read -r X Y W H < <(slop -f "%x %y %w %h" -b 1 -t 0 -q)
#(( W /= 6 ))
#(( H /= 16 ))
W="$(( W - 6 ))"
H="$(( H - 6 ))"
g=${W}x${H}+${X}+${Y}
tabbed -r 2 -c -g $g -n floating st -w ''
