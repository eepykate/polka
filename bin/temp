#!/bin/sh
#  make a tmp dir

trap 'rm -rf "$dir"' INT HUP TERM EXIT
dir="${XDG_CACHE_HOME:=$HOME/.cache}/tmp-$$"

mkdir -p "$dir"
cd "$dir"

HOME=$dir \
	XDG_DATA_HOME=$dir/usr \
	XDG_CACHE_HOME=$dir/var \
	XDG_CONFIG_HOME=$dir/etc \
	_JAVA_OPTIONS=-Duser.home=$dir \
	$SHELL
