#!/bin/sh

f() { feh -Z. -g 1000x700 --conversion-timeout 3 "$@"; }
[ "$#" -gt 1 ] && { f "$@"; exit; }

case "$1" in
	http*)  f "$1";;
	*)      f --scale-down --start-at "$1" "$(dirname "$1")";;
esac
