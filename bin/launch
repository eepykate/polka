#!/bin/sh

TERM='' IFS=:
set -- $PATH

# final pipe to cat is so the script/command doesn't think it's being ran in a terminal

find "$@" -type f,l -printf '%f\n' -executable \
	| sort | uniq | menu | sh | cat &
