#!/usr/bin/env bash

accent="#447bbe"
fglight="#ccccee"

curname="$(bspc query -N -n)"
for i in $(bspc query -N -n .window -d focused); do
	if [[ $curname = $i ]]; then
		echo -en "%{F$accent}⚫ " 
	else
		echo -n "%{F$fg}⚫ "
	fi
done
echo
