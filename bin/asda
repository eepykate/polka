#!/usr/bin/env bash

curname="$(bspc query -N -n)"
for i in $(bspc query -N -n .window -d focused); do
	if [[ $curname = $i ]]; then
		echo -en "%{F#ee99ff}⚫ " 
	else
		echo -n "%{F#ccccee}⚫ "
	fi
done
echo