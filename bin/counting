#!/usr/bin/env bash

date="$(date +%s)"
while true; do 
	newdate="$(date +%s)"
	clear
	xd="$(( $newdate - $date ))"
	
	M=$(($xd/60%60));   S=$(($xd%60))
	if [[ $M -ne 0 ]]; then
		time_passed="${M}m ${S}s"
	else 
		time_passed="${S}s"
	fi
	printf "$time_passed\n"
	sleep 1
done
