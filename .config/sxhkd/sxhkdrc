super + e
	st -e cmus

super + w
	st -c floating -e nmtui-connect

super + y
	st -e transmission-remote-cli

ctrl + Escape
	xkill

XF86MonBrightness{Down,Up}
	light -{U,A} 10

{super,ctrl} + alt + t
	synclient TouchpadOff={0,1}

super + Return
	st

super + f
	firefox-developer-edition

super + c
	dolphin

super + space
	rofi -show run

alt + ctrl + space
	dmenu_run

Print
	scr

super + Print
	scr -f

shift + Print
	scr -s

ctrl + alt + m
	dmenumount

ctrl + alt + u
	dmenuumount

ctrl + alt + Prior
	blaze -c -p k -B 8

ctrl + alt + Home
	blaze -c -p k

ctrl + alt + Insert
	blaze

ctrl + alt + End
	blaze -s

super + i
	rofi-emoji

XF86AudioPrev
	vol prev

XF86AudioNext
	vol next

XF86AudioPlay
	vol p

super + p
	vol p

XF86AudioRaiseVolume
	vol up

XF86AudioLowerVolume
	vol down

super + u
	vol up

super + j
	vol down

XF86AudioStop
	vol stop

XF86AudioMute
	vol m

ctrl + alt + w
	wifi

super + l
	lock

super + t
	tp.sh

shift + super + F1
	tp.sh

ctrl + alt + s
	ts.sh

alt + grave
	dropdown.sh

#alt + q
#	xdotool getwindowfocus windowclose

#super + q
#	xdotool getwindowfocus windowkill

super + s
	players.sh

alt + x
	select_st

#    ---
#   bspwm
#    ---

super + q
	bspc node -k

alt + q
	bspc node -c

alt + c
	bspc node -p cancel

#super + r
#	bspc node -n last.!automatic -t tiled

super + {r,m}
	bspc desktop -l {tiled,monocle}

super + {_,shift} + Escape
	wm {restart,quit}

super + {d,a}
	bspc node -t "~{floating,fullscreen}"

super + {_,ctrl,alt,shift} + {Left,Down,Up,Right}
	{focus,move,resize,bspc node -p} {west,south,north,east}

ctrl + alt + {Left,Right}
	desktop.sh switch {previous,next}

shift + alt + {Left,Right}
	desktop.sh move {previous,next}

shift + alt + {Right,Left}
	DIR={next,prev}; \
	bspc node -d "$DIR"; \
	bspc desktop -f "$DIR"

ctrl + alt + {Right,Left}
	bspc desktop -f {next,prev}

ctrl + super + {1,2,3,4,5,6,7}
	bspc node -d {1,2,3,4,5,6,7}; \
	bar -d

super + {1,2,3,4,5,6,7}
	bspc desktop -f {1,2,3,4,5,6,7}; \
	bar -d

alt + Tab
	bspc node -f next; \
	bar -d

alt + shift + Tab
	bspc node -f prev; \
	bar -d

super + alt + 0
	gaps r

super + alt + equal
	gaps up

super + alt + minus
	gaps down
