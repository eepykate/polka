uniform float      opacity;
uniform bool       invert_color;
uniform sampler2D  tex;

void
main()
{
	vec4   c;
	float  y;

	c = texture2D(tex, gl_TexCoord[0].xy);
	y = dot(c.rgb, vec3(0.299, 0.587, 0.114));
	gl_FragColor = vec4(y, y, y, 1.0);
}
