#!/bin/sh
exec 2>/dev/null

[ "$1" = y ] || {
	printf '[press the damn button, fool] '
	read -r y
}

[ "${1:-$y}" = y ] || exit

mkdir -p  \
	~/etc       \
	~/bin/app   \
	~/bin/bin   \
	~/opt/junk  \
	~/opt/apps  \
	~/usr/cache

ln -sf "$PWD/bin/"* ~/bin/
ln -sf "$PWD/etc/"* ~/etc/
ln -sf "$PWD/usr/"* ~/usr/
:
